<%- include('partials/header') -%>
<!-- OLD FEED PAGE -->
  <!-- <div class="container">
    <div class="row justify-content-center mt-5">
      <ul class="row list-unstyled">
        <% for(var i=0; i<posts.length; i++) {%>
          <li class="col-6 justify-content-between mt-5">
            <a href="/post/<%= posts[i]._id%>">
              <img class="img-fluid" src="<%= posts[i].image%>">
            </a>
          </li>
        <% } %>
        </ul>
    </div>
  </div>   -->
  <!-- NEW SELECTION PAGE -->
  
  <div class='peanut' include="form-input-select()">
    <select required onchange="location = this.value">
      <!--
        This is how we can do "placeholder" options.
        note: "required" attribute is on the select
      -->
      <option value=""
              hidden
      >Skill Level</option>
 
      <!-- normal options -->

      <option value="start.ejs">Just Starting</option>
      <option value="beginner.ejs">Beginner</option>
      <option value="intermediate.ejs">Intermediate</option>
      <option value="gnarly.ejs">Gnarly</option>
      
    </select>
  </div>
  
  
  
<%- include('partials/footer') -%>